def insertionSort2(n, arr):
    # Write your code here
    for i in range(1,n):
        temp=arr[i]
        k=i
        while k>0 and arr[k-1]>=temp:
            arr[k]=arr[k-1]
            k-=1
        arr[k]=temp
        print(*arr)
